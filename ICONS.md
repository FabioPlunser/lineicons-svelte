# Lineicons Svelte - Icon Reference

This document contains a complete list of all 606 icons available in the `lineicons-svelte` package.

## Quick Start

```svelte
<script>
  import { Home2, Heart, Search1 } from 'lineicons-svelte';
</script>

<Home2 size={24} color="currentColor" />
<Heart size={32} color="red" />
<Search1 class="my-icon" />
```

## Props

All icons accept the following props:

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `number \| string` | `24` | Width and height of the icon |
| `color` | `string` | `'currentColor'` | Fill color of the icon |
| `class` | `string` | `''` | Additional CSS classes |
| `...rest` | `object` | `{}` | Any other SVG attributes |

## Icon List

### 5

| Icon | Import |
|------|--------|
| `500px` | `import { 500px } from 'lineicons-svelte';` |

### A

| Icon | Import |
|------|--------|
| `adobe` | `import { adobe } from 'lineicons-svelte';` |
| `adonis` | `import { adonis } from 'lineicons-svelte';` |
| `aeroplane-1` | `import { aeroplane-1 } from 'lineicons-svelte';` |
| `agenda` | `import { agenda } from 'lineicons-svelte';` |
| `airbnb` | `import { airbnb } from 'lineicons-svelte';` |
| `airtable` | `import { airtable } from 'lineicons-svelte';` |
| `alarm-1` | `import { alarm-1 } from 'lineicons-svelte';` |
| `align-text-center` | `import { align-text-center } from 'lineicons-svelte';` |
| `align-text-left` | `import { align-text-left } from 'lineicons-svelte';` |
| `align-text-right` | `import { align-text-right } from 'lineicons-svelte';` |
| `alpinejs` | `import { alpinejs } from 'lineicons-svelte';` |
| `amazon` | `import { amazon } from 'lineicons-svelte';` |
| `amazon-original` | `import { amazon-original } from 'lineicons-svelte';` |
| `amazon-pay` | `import { amazon-pay } from 'lineicons-svelte';` |
| `ambulance-1` | `import { ambulance-1 } from 'lineicons-svelte';` |
| `amd` | `import { amd } from 'lineicons-svelte';` |
| `amex` | `import { amex } from 'lineicons-svelte';` |
| `anchor` | `import { anchor } from 'lineicons-svelte';` |
| `android` | `import { android } from 'lineicons-svelte';` |
| `android-old` | `import { android-old } from 'lineicons-svelte';` |
| `angellist` | `import { angellist } from 'lineicons-svelte';` |
| `angle-double-down` | `import { angle-double-down } from 'lineicons-svelte';` |
| `angle-double-left` | `import { angle-double-left } from 'lineicons-svelte';` |
| `angle-double-right` | `import { angle-double-right } from 'lineicons-svelte';` |
| `angle-double-up` | `import { angle-double-up } from 'lineicons-svelte';` |
| `angular` | `import { angular } from 'lineicons-svelte';` |
| `app-store` | `import { app-store } from 'lineicons-svelte';` |
| `apple-brand` | `import { apple-brand } from 'lineicons-svelte';` |
| `apple-music` | `import { apple-music } from 'lineicons-svelte';` |
| `apple-music-alt` | `import { apple-music-alt } from 'lineicons-svelte';` |
| `apple-pay` | `import { apple-pay } from 'lineicons-svelte';` |
| `arc-browser` | `import { arc-browser } from 'lineicons-svelte';` |
| `arrow-all-direction` | `import { arrow-all-direction } from 'lineicons-svelte';` |
| `arrow-angular-top-left` | `import { arrow-angular-top-left } from 'lineicons-svelte';` |
| `arrow-angular-top-right` | `import { arrow-angular-top-right } from 'lineicons-svelte';` |
| `arrow-both-direction-horizontal-1` | `import { arrow-both-direction-horizontal-1 } from 'lineicons-svelte';` |
| `arrow-both-direction-vertical-1` | `import { arrow-both-direction-vertical-1 } from 'lineicons-svelte';` |
| `arrow-downward` | `import { arrow-downward } from 'lineicons-svelte';` |
| `arrow-left` | `import { arrow-left } from 'lineicons-svelte';` |
| `arrow-left-circle` | `import { arrow-left-circle } from 'lineicons-svelte';` |
| `arrow-right` | `import { arrow-right } from 'lineicons-svelte';` |
| `arrow-right-circle` | `import { arrow-right-circle } from 'lineicons-svelte';` |
| `arrow-upward` | `import { arrow-upward } from 'lineicons-svelte';` |
| `asana` | `import { asana } from 'lineicons-svelte';` |
| `astro` | `import { astro } from 'lineicons-svelte';` |
| `atlassian` | `import { atlassian } from 'lineicons-svelte';` |
| `audi` | `import { audi } from 'lineicons-svelte';` |
| `audi-alt` | `import { audi-alt } from 'lineicons-svelte';` |
| `aws` | `import { aws } from 'lineicons-svelte';` |
| `azure` | `import { azure } from 'lineicons-svelte';` |

### B

| Icon | Import |
|------|--------|
| `badge-decagram-percent` | `import { badge-decagram-percent } from 'lineicons-svelte';` |
| `balloons` | `import { balloons } from 'lineicons-svelte';` |
| `ban-2` | `import { ban-2 } from 'lineicons-svelte';` |
| `bar-chart-4` | `import { bar-chart-4 } from 'lineicons-svelte';` |
| `bar-chart-dollar` | `import { bar-chart-dollar } from 'lineicons-svelte';` |
| `basket-shopping-3` | `import { basket-shopping-3 } from 'lineicons-svelte';` |
| `beat` | `import { beat } from 'lineicons-svelte';` |
| `behance` | `import { behance } from 'lineicons-svelte';` |
| `bell-1` | `import { bell-1 } from 'lineicons-svelte';` |
| `bike` | `import { bike } from 'lineicons-svelte';` |
| `bing` | `import { bing } from 'lineicons-svelte';` |
| `bitbucket` | `import { bitbucket } from 'lineicons-svelte';` |
| `bitcoin` | `import { bitcoin } from 'lineicons-svelte';` |
| `bittorrent` | `import { bittorrent } from 'lineicons-svelte';` |
| `blogger` | `import { blogger } from 'lineicons-svelte';` |
| `blogger-alt` | `import { blogger-alt } from 'lineicons-svelte';` |
| `bluetooth` | `import { bluetooth } from 'lineicons-svelte';` |
| `bluetooth-logo` | `import { bluetooth-logo } from 'lineicons-svelte';` |
| `bmw` | `import { bmw } from 'lineicons-svelte';` |
| `board-writing-3` | `import { board-writing-3 } from 'lineicons-svelte';` |
| `bold` | `import { bold } from 'lineicons-svelte';` |
| `bolt-2` | `import { bolt-2 } from 'lineicons-svelte';` |
| `bolt-3` | `import { bolt-3 } from 'lineicons-svelte';` |
| `book-1` | `import { book-1 } from 'lineicons-svelte';` |
| `bookmark-1` | `import { bookmark-1 } from 'lineicons-svelte';` |
| `bookmark-circle` | `import { bookmark-circle } from 'lineicons-svelte';` |
| `books-2` | `import { books-2 } from 'lineicons-svelte';` |
| `bootstrap-5` | `import { bootstrap-5 } from 'lineicons-svelte';` |
| `bootstrap-5-square` | `import { bootstrap-5-square } from 'lineicons-svelte';` |
| `box-archive-1` | `import { box-archive-1 } from 'lineicons-svelte';` |
| `box-closed` | `import { box-closed } from 'lineicons-svelte';` |
| `box-gift-1` | `import { box-gift-1 } from 'lineicons-svelte';` |
| `brave` | `import { brave } from 'lineicons-svelte';` |
| `bricks` | `import { bricks } from 'lineicons-svelte';` |
| `bridge-3` | `import { bridge-3 } from 'lineicons-svelte';` |
| `briefcase-1` | `import { briefcase-1 } from 'lineicons-svelte';` |
| `briefcase-2` | `import { briefcase-2 } from 'lineicons-svelte';` |
| `briefcase-plus-1` | `import { briefcase-plus-1 } from 'lineicons-svelte';` |
| `brush-1-rotated` | `import { brush-1-rotated } from 'lineicons-svelte';` |
| `brush-2` | `import { brush-2 } from 'lineicons-svelte';` |
| `btc` | `import { btc } from 'lineicons-svelte';` |
| `bug-1` | `import { bug-1 } from 'lineicons-svelte';` |
| `buildings-1` | `import { buildings-1 } from 'lineicons-svelte';` |
| `bulb-2` | `import { bulb-2 } from 'lineicons-svelte';` |
| `bulb-4` | `import { bulb-4 } from 'lineicons-svelte';` |
| `burger-1` | `import { burger-1 } from 'lineicons-svelte';` |
| `burger-drink` | `import { burger-drink } from 'lineicons-svelte';` |
| `bus-1` | `import { bus-1 } from 'lineicons-svelte';` |
| `busket-ball` | `import { busket-ball } from 'lineicons-svelte';` |

### C

| Icon | Import |
|------|--------|
| `cake-1` | `import { cake-1 } from 'lineicons-svelte';` |
| `calculator-1` | `import { calculator-1 } from 'lineicons-svelte';` |
| `calculator-2` | `import { calculator-2 } from 'lineicons-svelte';` |
| `calendar-days` | `import { calendar-days } from 'lineicons-svelte';` |
| `camera-1` | `import { camera-1 } from 'lineicons-svelte';` |
| `camera-movie-1` | `import { camera-movie-1 } from 'lineicons-svelte';` |
| `candy-cane-2` | `import { candy-cane-2 } from 'lineicons-svelte';` |
| `candy-round-1` | `import { candy-round-1 } from 'lineicons-svelte';` |
| `canva` | `import { canva } from 'lineicons-svelte';` |
| `capsule-1` | `import { capsule-1 } from 'lineicons-svelte';` |
| `car-2` | `import { car-2 } from 'lineicons-svelte';` |
| `car-4` | `import { car-4 } from 'lineicons-svelte';` |
| `car-6` | `import { car-6 } from 'lineicons-svelte';` |
| `caravan-1` | `import { caravan-1 } from 'lineicons-svelte';` |
| `cart-1` | `import { cart-1 } from 'lineicons-svelte';` |
| `cart-2` | `import { cart-2 } from 'lineicons-svelte';` |
| `cash-app` | `import { cash-app } from 'lineicons-svelte';` |
| `certificate-badge-1` | `import { certificate-badge-1 } from 'lineicons-svelte';` |
| `chat-bubble-2` | `import { chat-bubble-2 } from 'lineicons-svelte';` |
| `check` | `import { check } from 'lineicons-svelte';` |
| `check-circle-1` | `import { check-circle-1 } from 'lineicons-svelte';` |
| `check-square-2` | `import { check-square-2 } from 'lineicons-svelte';` |
| `chevron-down` | `import { chevron-down } from 'lineicons-svelte';` |
| `chevron-down-circle` | `import { chevron-down-circle } from 'lineicons-svelte';` |
| `chevron-left` | `import { chevron-left } from 'lineicons-svelte';` |
| `chevron-left-circle` | `import { chevron-left-circle } from 'lineicons-svelte';` |
| `chevron-right-circle` | `import { chevron-right-circle } from 'lineicons-svelte';` |
| `chevron-up` | `import { chevron-up } from 'lineicons-svelte';` |
| `chevron-up-circle` | `import { chevron-up-circle } from 'lineicons-svelte';` |
| `chrome` | `import { chrome } from 'lineicons-svelte';` |
| `chromecast` | `import { chromecast } from 'lineicons-svelte';` |
| `cisco` | `import { cisco } from 'lineicons-svelte';` |
| `claude` | `import { claude } from 'lineicons-svelte';` |
| `clickup` | `import { clickup } from 'lineicons-svelte';` |
| `clipboard` | `import { clipboard } from 'lineicons-svelte';` |
| `cloud-2` | `import { cloud-2 } from 'lineicons-svelte';` |
| `cloud-bolt-1` | `import { cloud-bolt-1 } from 'lineicons-svelte';` |
| `cloud-bolt-2` | `import { cloud-bolt-2 } from 'lineicons-svelte';` |
| `cloud-check-circle` | `import { cloud-check-circle } from 'lineicons-svelte';` |
| `cloud-download` | `import { cloud-download } from 'lineicons-svelte';` |
| `cloud-iot-2` | `import { cloud-iot-2 } from 'lineicons-svelte';` |
| `cloud-rain` | `import { cloud-rain } from 'lineicons-svelte';` |
| `cloud-refresh-clockwise` | `import { cloud-refresh-clockwise } from 'lineicons-svelte';` |
| `cloud-sun` | `import { cloud-sun } from 'lineicons-svelte';` |
| `cloud-upload` | `import { cloud-upload } from 'lineicons-svelte';` |
| `cloudflare` | `import { cloudflare } from 'lineicons-svelte';` |
| `code-1` | `import { code-1 } from 'lineicons-svelte';` |
| `code-s` | `import { code-s } from 'lineicons-svelte';` |
| `codepen` | `import { codepen } from 'lineicons-svelte';` |
| `coffee-cup-2` | `import { coffee-cup-2 } from 'lineicons-svelte';` |
| `coinbase` | `import { coinbase } from 'lineicons-svelte';` |
| `colour-palette-3` | `import { colour-palette-3 } from 'lineicons-svelte';` |
| `comment-1` | `import { comment-1 } from 'lineicons-svelte';` |
| `comment-1-share` | `import { comment-1-share } from 'lineicons-svelte';` |
| `comment-1-text` | `import { comment-1-text } from 'lineicons-svelte';` |
| `compass-drafting-2` | `import { compass-drafting-2 } from 'lineicons-svelte';` |
| `connectdevelop` | `import { connectdevelop } from 'lineicons-svelte';` |
| `copilot` | `import { copilot } from 'lineicons-svelte';` |
| `coral` | `import { coral } from 'lineicons-svelte';` |
| `cpanel` | `import { cpanel } from 'lineicons-svelte';` |
| `crane-4` | `import { crane-4 } from 'lineicons-svelte';` |
| `creative-commons` | `import { creative-commons } from 'lineicons-svelte';` |
| `credit-card-multiple` | `import { credit-card-multiple } from 'lineicons-svelte';` |
| `crop-2` | `import { crop-2 } from 'lineicons-svelte';` |
| `crown-3` | `import { crown-3 } from 'lineicons-svelte';` |
| `css3` | `import { css3 } from 'lineicons-svelte';` |

### D

| Icon | Import |
|------|--------|
| `dashboard-square-1` | `import { dashboard-square-1 } from 'lineicons-svelte';` |
| `database-2` | `import { database-2 } from 'lineicons-svelte';` |
| `deno` | `import { deno } from 'lineicons-svelte';` |
| `dev` | `import { dev } from 'lineicons-svelte';` |
| `dialogflow` | `import { dialogflow } from 'lineicons-svelte';` |
| `diamonds-1` | `import { diamonds-1 } from 'lineicons-svelte';` |
| `diamonds-2` | `import { diamonds-2 } from 'lineicons-svelte';` |
| `digitalocean` | `import { digitalocean } from 'lineicons-svelte';` |
| `diners-club` | `import { diners-club } from 'lineicons-svelte';` |
| `direction-ltr` | `import { direction-ltr } from 'lineicons-svelte';` |
| `direction-rtl` | `import { direction-rtl } from 'lineicons-svelte';` |
| `discord` | `import { discord } from 'lineicons-svelte';` |
| `discord-chat` | `import { discord-chat } from 'lineicons-svelte';` |
| `discover` | `import { discover } from 'lineicons-svelte';` |
| `docker` | `import { docker } from 'lineicons-svelte';` |
| `dollar` | `import { dollar } from 'lineicons-svelte';` |
| `dollar-circle` | `import { dollar-circle } from 'lineicons-svelte';` |
| `double-quotes-end-1` | `import { double-quotes-end-1 } from 'lineicons-svelte';` |
| `download-1` | `import { download-1 } from 'lineicons-svelte';` |
| `download-circle-1` | `import { download-circle-1 } from 'lineicons-svelte';` |
| `dribbble` | `import { dribbble } from 'lineicons-svelte';` |
| `dribbble-symbol` | `import { dribbble-symbol } from 'lineicons-svelte';` |
| `drizzle` | `import { drizzle } from 'lineicons-svelte';` |
| `dropbox` | `import { dropbox } from 'lineicons-svelte';` |
| `drupal` | `import { drupal } from 'lineicons-svelte';` |
| `dumbbell-1` | `import { dumbbell-1 } from 'lineicons-svelte';` |

### E

| Icon | Import |
|------|--------|
| `edge` | `import { edge } from 'lineicons-svelte';` |
| `emoji-expressionless` | `import { emoji-expressionless } from 'lineicons-svelte';` |
| `emoji-expressionless-flat-eyes` | `import { emoji-expressionless-flat-eyes } from 'lineicons-svelte';` |
| `emoji-grin` | `import { emoji-grin } from 'lineicons-svelte';` |
| `emoji-sad` | `import { emoji-sad } from 'lineicons-svelte';` |
| `emoji-smile` | `import { emoji-smile } from 'lineicons-svelte';` |
| `emoji-smile-side` | `import { emoji-smile-side } from 'lineicons-svelte';` |
| `emoji-smile-sunglass` | `import { emoji-smile-sunglass } from 'lineicons-svelte';` |
| `emoji-smile-tongue` | `import { emoji-smile-tongue } from 'lineicons-svelte';` |
| `enter` | `import { enter } from 'lineicons-svelte';` |
| `enter-down` | `import { enter-down } from 'lineicons-svelte';` |
| `envato` | `import { envato } from 'lineicons-svelte';` |
| `envelope-1` | `import { envelope-1 } from 'lineicons-svelte';` |
| `eraser-1` | `import { eraser-1 } from 'lineicons-svelte';` |
| `ethereum-logo` | `import { ethereum-logo } from 'lineicons-svelte';` |
| `euro` | `import { euro } from 'lineicons-svelte';` |
| `exit` | `import { exit } from 'lineicons-svelte';` |
| `exit-up` | `import { exit-up } from 'lineicons-svelte';` |
| `expand-arrow-1` | `import { expand-arrow-1 } from 'lineicons-svelte';` |
| `expand-square-4` | `import { expand-square-4 } from 'lineicons-svelte';` |
| `expressjs` | `import { expressjs } from 'lineicons-svelte';` |
| `eye` | `import { eye } from 'lineicons-svelte';` |

### F

| Icon | Import |
|------|--------|
| `facebook` | `import { facebook } from 'lineicons-svelte';` |
| `facebook-messenger` | `import { facebook-messenger } from 'lineicons-svelte';` |
| `facebook-rounded` | `import { facebook-rounded } from 'lineicons-svelte';` |
| `facebook-square` | `import { facebook-square } from 'lineicons-svelte';` |
| `facetime` | `import { facetime } from 'lineicons-svelte';` |
| `figma` | `import { figma } from 'lineicons-svelte';` |
| `file-format-zip` | `import { file-format-zip } from 'lineicons-svelte';` |
| `file-multiple` | `import { file-multiple } from 'lineicons-svelte';` |
| `file-pencil` | `import { file-pencil } from 'lineicons-svelte';` |
| `file-plus-circle` | `import { file-plus-circle } from 'lineicons-svelte';` |
| `file-question` | `import { file-question } from 'lineicons-svelte';` |
| `file-xmark` | `import { file-xmark } from 'lineicons-svelte';` |
| `firebase` | `import { firebase } from 'lineicons-svelte';` |
| `firefox` | `import { firefox } from 'lineicons-svelte';` |
| `firework-rocket-4` | `import { firework-rocket-4 } from 'lineicons-svelte';` |
| `fitbit` | `import { fitbit } from 'lineicons-svelte';` |
| `flag-1` | `import { flag-1 } from 'lineicons-svelte';` |
| `flag-2` | `import { flag-2 } from 'lineicons-svelte';` |
| `flickr` | `import { flickr } from 'lineicons-svelte';` |
| `floppy-disk-1` | `import { floppy-disk-1 } from 'lineicons-svelte';` |
| `flower-2` | `import { flower-2 } from 'lineicons-svelte';` |
| `flutter` | `import { flutter } from 'lineicons-svelte';` |
| `folder-1` | `import { folder-1 } from 'lineicons-svelte';` |
| `ford` | `import { ford } from 'lineicons-svelte';` |
| `framer` | `import { framer } from 'lineicons-svelte';` |
| `funnel-1` | `import { funnel-1 } from 'lineicons-svelte';` |

### G

| Icon | Import |
|------|--------|
| `gallery` | `import { gallery } from 'lineicons-svelte';` |
| `game-pad-modern-1` | `import { game-pad-modern-1 } from 'lineicons-svelte';` |
| `gatsby` | `import { gatsby } from 'lineicons-svelte';` |
| `gauge-1` | `import { gauge-1 } from 'lineicons-svelte';` |
| `gear-1` | `import { gear-1 } from 'lineicons-svelte';` |
| `gears-3` | `import { gears-3 } from 'lineicons-svelte';` |
| `gemini` | `import { gemini } from 'lineicons-svelte';` |
| `git` | `import { git } from 'lineicons-svelte';` |
| `github` | `import { github } from 'lineicons-svelte';` |
| `glass-juice-1` | `import { glass-juice-1 } from 'lineicons-svelte';` |
| `globe-1` | `import { globe-1 } from 'lineicons-svelte';` |
| `globe-stand` | `import { globe-stand } from 'lineicons-svelte';` |
| `go` | `import { go } from 'lineicons-svelte';` |
| `goodreads` | `import { goodreads } from 'lineicons-svelte';` |
| `google` | `import { google } from 'lineicons-svelte';` |
| `google-cloud` | `import { google-cloud } from 'lineicons-svelte';` |
| `google-drive` | `import { google-drive } from 'lineicons-svelte';` |
| `google-meet` | `import { google-meet } from 'lineicons-svelte';` |
| `google-pay` | `import { google-pay } from 'lineicons-svelte';` |
| `google-wallet` | `import { google-wallet } from 'lineicons-svelte';` |
| `graduation-cap-1` | `import { graduation-cap-1 } from 'lineicons-svelte';` |
| `grammarly` | `import { grammarly } from 'lineicons-svelte';` |

### H

| Icon | Import |
|------|--------|
| `hacker-news` | `import { hacker-news } from 'lineicons-svelte';` |
| `hammer-1` | `import { hammer-1 } from 'lineicons-svelte';` |
| `hammer-2` | `import { hammer-2 } from 'lineicons-svelte';` |
| `hand-mic` | `import { hand-mic } from 'lineicons-svelte';` |
| `hand-shake` | `import { hand-shake } from 'lineicons-svelte';` |
| `hand-stop` | `import { hand-stop } from 'lineicons-svelte';` |
| `hand-taking-dollar` | `import { hand-taking-dollar } from 'lineicons-svelte';` |
| `hand-taking-leaf-1` | `import { hand-taking-leaf-1 } from 'lineicons-svelte';` |
| `hand-taking-user` | `import { hand-taking-user } from 'lineicons-svelte';` |
| `hashnode` | `import { hashnode } from 'lineicons-svelte';` |
| `hat-chef-3` | `import { hat-chef-3 } from 'lineicons-svelte';` |
| `headphone-1` | `import { headphone-1 } from 'lineicons-svelte';` |
| `heart` | `import { heart } from 'lineicons-svelte';` |
| `helicopter-2` | `import { helicopter-2 } from 'lineicons-svelte';` |
| `helmet-safety-1` | `import { helmet-safety-1 } from 'lineicons-svelte';` |
| `hierarchy-1` | `import { hierarchy-1 } from 'lineicons-svelte';` |
| `highlighter-1` | `import { highlighter-1 } from 'lineicons-svelte';` |
| `highlighter-2` | `import { highlighter-2 } from 'lineicons-svelte';` |
| `home-2` | `import { home-2 } from 'lineicons-svelte';` |
| `hospital-2` | `import { hospital-2 } from 'lineicons-svelte';` |
| `hourglass` | `import { hourglass } from 'lineicons-svelte';` |
| `html5` | `import { html5 } from 'lineicons-svelte';` |

### I

| Icon | Import |
|------|--------|
| `ibm` | `import { ibm } from 'lineicons-svelte';` |
| `id-card` | `import { id-card } from 'lineicons-svelte';` |
| `imdb` | `import { imdb } from 'lineicons-svelte';` |
| `indent` | `import { indent } from 'lineicons-svelte';` |
| `info` | `import { info } from 'lineicons-svelte';` |
| `injection-1` | `import { injection-1 } from 'lineicons-svelte';` |
| `instagram` | `import { instagram } from 'lineicons-svelte';` |
| `instagram-logotype` | `import { instagram-logotype } from 'lineicons-svelte';` |
| `intel` | `import { intel } from 'lineicons-svelte';` |
| `ios` | `import { ios } from 'lineicons-svelte';` |
| `island-2` | `import { island-2 } from 'lineicons-svelte';` |

### J

| Icon | Import |
|------|--------|
| `jaguar` | `import { jaguar } from 'lineicons-svelte';` |
| `jamstack` | `import { jamstack } from 'lineicons-svelte';` |
| `java` | `import { java } from 'lineicons-svelte';` |
| `javascript` | `import { javascript } from 'lineicons-svelte';` |
| `jcb` | `import { jcb } from 'lineicons-svelte';` |
| `joomla` | `import { joomla } from 'lineicons-svelte';` |
| `jsfiddle` | `import { jsfiddle } from 'lineicons-svelte';` |

### K

| Icon | Import |
|------|--------|
| `key-1` | `import { key-1 } from 'lineicons-svelte';` |
| `keyboard` | `import { keyboard } from 'lineicons-svelte';` |
| `knife-fork-1` | `import { knife-fork-1 } from 'lineicons-svelte';` |
| `kubernetes` | `import { kubernetes } from 'lineicons-svelte';` |

### L

| Icon | Import |
|------|--------|
| `label-dollar-2` | `import { label-dollar-2 } from 'lineicons-svelte';` |
| `laptop-2` | `import { laptop-2 } from 'lineicons-svelte';` |
| `laptop-phone` | `import { laptop-phone } from 'lineicons-svelte';` |
| `laravel` | `import { laravel } from 'lineicons-svelte';` |
| `layers-1` | `import { layers-1 } from 'lineicons-svelte';` |
| `layout-26` | `import { layout-26 } from 'lineicons-svelte';` |
| `layout-9` | `import { layout-9 } from 'lineicons-svelte';` |
| `leaf-1` | `import { leaf-1 } from 'lineicons-svelte';` |
| `leaf-6` | `import { leaf-6 } from 'lineicons-svelte';` |
| `lemon-squeezy` | `import { lemon-squeezy } from 'lineicons-svelte';` |
| `life-guard-tube-1` | `import { life-guard-tube-1 } from 'lineicons-svelte';` |
| `line` | `import { line } from 'lineicons-svelte';` |
| `line-dashed` | `import { line-dashed } from 'lineicons-svelte';` |
| `line-dotted` | `import { line-dotted } from 'lineicons-svelte';` |
| `line-height` | `import { line-height } from 'lineicons-svelte';` |
| `lineicons` | `import { lineicons } from 'lineicons-svelte';` |
| `link-2-angular-right` | `import { link-2-angular-right } from 'lineicons-svelte';` |
| `linkedin` | `import { linkedin } from 'lineicons-svelte';` |
| `location-arrow-right` | `import { location-arrow-right } from 'lineicons-svelte';` |
| `locked-1` | `import { locked-1 } from 'lineicons-svelte';` |
| `locked-2` | `import { locked-2 } from 'lineicons-svelte';` |
| `loom` | `import { loom } from 'lineicons-svelte';` |

### M

| Icon | Import |
|------|--------|
| `magento` | `import { magento } from 'lineicons-svelte';` |
| `magnet` | `import { magnet } from 'lineicons-svelte';` |
| `mailchimp` | `import { mailchimp } from 'lineicons-svelte';` |
| `map-marker-1` | `import { map-marker-1 } from 'lineicons-svelte';` |
| `map-marker-5` | `import { map-marker-5 } from 'lineicons-svelte';` |
| `map-pin-5` | `import { map-pin-5 } from 'lineicons-svelte';` |
| `markdown` | `import { markdown } from 'lineicons-svelte';` |
| `mastercard` | `import { mastercard } from 'lineicons-svelte';` |
| `medium` | `import { medium } from 'lineicons-svelte';` |
| `medium-alt` | `import { medium-alt } from 'lineicons-svelte';` |
| `megaphone-1` | `import { megaphone-1 } from 'lineicons-svelte';` |
| `menu-cheesburger` | `import { menu-cheesburger } from 'lineicons-svelte';` |
| `menu-hamburger-1` | `import { menu-hamburger-1 } from 'lineicons-svelte';` |
| `menu-meatballs-1` | `import { menu-meatballs-1 } from 'lineicons-svelte';` |
| `menu-meatballs-2` | `import { menu-meatballs-2 } from 'lineicons-svelte';` |
| `mercedes` | `import { mercedes } from 'lineicons-svelte';` |
| `message-2` | `import { message-2 } from 'lineicons-svelte';` |
| `message-2-question` | `import { message-2-question } from 'lineicons-svelte';` |
| `message-3-text` | `import { message-3-text } from 'lineicons-svelte';` |
| `meta` | `import { meta } from 'lineicons-svelte';` |
| `meta-alt` | `import { meta-alt } from 'lineicons-svelte';` |
| `microphone-1` | `import { microphone-1 } from 'lineicons-svelte';` |
| `microscope` | `import { microscope } from 'lineicons-svelte';` |
| `microsoft` | `import { microsoft } from 'lineicons-svelte';` |
| `microsoft-edge` | `import { microsoft-edge } from 'lineicons-svelte';` |
| `microsoft-teams` | `import { microsoft-teams } from 'lineicons-svelte';` |
| `minus` | `import { minus } from 'lineicons-svelte';` |
| `minus-circle` | `import { minus-circle } from 'lineicons-svelte';` |
| `mongodb` | `import { mongodb } from 'lineicons-svelte';` |
| `monitor` | `import { monitor } from 'lineicons-svelte';` |
| `monitor-code` | `import { monitor-code } from 'lineicons-svelte';` |
| `monitor-mac` | `import { monitor-mac } from 'lineicons-svelte';` |
| `moon-half-right-5` | `import { moon-half-right-5 } from 'lineicons-svelte';` |
| `mountains-2` | `import { mountains-2 } from 'lineicons-svelte';` |
| `mouse-2` | `import { mouse-2 } from 'lineicons-svelte';` |
| `mushroom-1` | `import { mushroom-1 } from 'lineicons-svelte';` |
| `mushroom-5` | `import { mushroom-5 } from 'lineicons-svelte';` |
| `music` | `import { music } from 'lineicons-svelte';` |
| `mysql` | `import { mysql } from 'lineicons-svelte';` |

### N

| Icon | Import |
|------|--------|
| `nasa` | `import { nasa } from 'lineicons-svelte';` |
| `netflix` | `import { netflix } from 'lineicons-svelte';` |
| `netlify` | `import { netlify } from 'lineicons-svelte';` |
| `next-step-2` | `import { next-step-2 } from 'lineicons-svelte';` |
| `nextjs` | `import { nextjs } from 'lineicons-svelte';` |
| `nike` | `import { nike } from 'lineicons-svelte';` |
| `nissan` | `import { nissan } from 'lineicons-svelte';` |
| `nodejs` | `import { nodejs } from 'lineicons-svelte';` |
| `nodejs-alt` | `import { nodejs-alt } from 'lineicons-svelte';` |
| `notebook-1` | `import { notebook-1 } from 'lineicons-svelte';` |
| `notion` | `import { notion } from 'lineicons-svelte';` |
| `npm` | `import { npm } from 'lineicons-svelte';` |
| `nuxt` | `import { nuxt } from 'lineicons-svelte';` |
| `nvidia` | `import { nvidia } from 'lineicons-svelte';` |

### O

| Icon | Import |
|------|--------|
| `oculus` | `import { oculus } from 'lineicons-svelte';` |
| `open-ai` | `import { open-ai } from 'lineicons-svelte';` |
| `opera-mini` | `import { opera-mini } from 'lineicons-svelte';` |
| `oracle` | `import { oracle } from 'lineicons-svelte';` |
| `outdent` | `import { outdent } from 'lineicons-svelte';` |

### P

| Icon | Import |
|------|--------|
| `paddle` | `import { paddle } from 'lineicons-svelte';` |
| `page-break-1` | `import { page-break-1 } from 'lineicons-svelte';` |
| `pagination` | `import { pagination } from 'lineicons-svelte';` |
| `paint-bucket` | `import { paint-bucket } from 'lineicons-svelte';` |
| `paint-roller-1` | `import { paint-roller-1 } from 'lineicons-svelte';` |
| `paperclip-1` | `import { paperclip-1 } from 'lineicons-svelte';` |
| `party-flags` | `import { party-flags } from 'lineicons-svelte';` |
| `party-spray` | `import { party-spray } from 'lineicons-svelte';` |
| `patreon` | `import { patreon } from 'lineicons-svelte';` |
| `pause` | `import { pause } from 'lineicons-svelte';` |
| `payoneer` | `import { payoneer } from 'lineicons-svelte';` |
| `paypal` | `import { paypal } from 'lineicons-svelte';` |
| `pen-to-square` | `import { pen-to-square } from 'lineicons-svelte';` |
| `pencil-1` | `import { pencil-1 } from 'lineicons-svelte';` |
| `pepsi` | `import { pepsi } from 'lineicons-svelte';` |
| `phone` | `import { phone } from 'lineicons-svelte';` |
| `photos` | `import { photos } from 'lineicons-svelte';` |
| `php` | `import { php } from 'lineicons-svelte';` |
| `pie-chart-2` | `import { pie-chart-2 } from 'lineicons-svelte';` |
| `pilcrow` | `import { pilcrow } from 'lineicons-svelte';` |
| `pimjo-logo` | `import { pimjo-logo } from 'lineicons-svelte';` |
| `pimjo-symbol` | `import { pimjo-symbol } from 'lineicons-svelte';` |
| `pinterest` | `import { pinterest } from 'lineicons-svelte';` |
| `pizza-2` | `import { pizza-2 } from 'lineicons-svelte';` |
| `placeholder-dollar` | `import { placeholder-dollar } from 'lineicons-svelte';` |
| `plantscale` | `import { plantscale } from 'lineicons-svelte';` |
| `play` | `import { play } from 'lineicons-svelte';` |
| `play-store` | `import { play-store } from 'lineicons-svelte';` |
| `playstation` | `import { playstation } from 'lineicons-svelte';` |
| `plug-1` | `import { plug-1 } from 'lineicons-svelte';` |
| `plus` | `import { plus } from 'lineicons-svelte';` |
| `plus-circle` | `import { plus-circle } from 'lineicons-svelte';` |
| `pnpm` | `import { pnpm } from 'lineicons-svelte';` |
| `postgresql` | `import { postgresql } from 'lineicons-svelte';` |
| `postman` | `import { postman } from 'lineicons-svelte';` |
| `pound` | `import { pound } from 'lineicons-svelte';` |
| `power-button` | `import { power-button } from 'lineicons-svelte';` |
| `previous-step-2` | `import { previous-step-2 } from 'lineicons-svelte';` |
| `printer` | `import { printer } from 'lineicons-svelte';` |
| `prisma` | `import { prisma } from 'lineicons-svelte';` |
| `producthunt` | `import { producthunt } from 'lineicons-svelte';` |
| `proton-mail-logo` | `import { proton-mail-logo } from 'lineicons-svelte';` |
| `proton-mail-symbol` | `import { proton-mail-symbol } from 'lineicons-svelte';` |
| `python` | `import { python } from 'lineicons-svelte';` |

### Q

| Icon | Import |
|------|--------|
| `question-mark` | `import { question-mark } from 'lineicons-svelte';` |
| `question-mark-circle` | `import { question-mark-circle } from 'lineicons-svelte';` |
| `quora` | `import { quora } from 'lineicons-svelte';` |

### R

| Icon | Import |
|------|--------|
| `radis` | `import { radis } from 'lineicons-svelte';` |
| `react` | `import { react } from 'lineicons-svelte';` |
| `reddit` | `import { reddit } from 'lineicons-svelte';` |
| `refresh-circle-1-clockwise` | `import { refresh-circle-1-clockwise } from 'lineicons-svelte';` |
| `refresh-dollar-1` | `import { refresh-dollar-1 } from 'lineicons-svelte';` |
| `refresh-user-1` | `import { refresh-user-1 } from 'lineicons-svelte';` |
| `remix-js` | `import { remix-js } from 'lineicons-svelte';` |
| `road-1` | `import { road-1 } from 'lineicons-svelte';` |
| `rocket-5` | `import { rocket-5 } from 'lineicons-svelte';` |
| `route-1` | `import { route-1 } from 'lineicons-svelte';` |
| `rss-right` | `import { rss-right } from 'lineicons-svelte';` |
| `ruler-1` | `import { ruler-1 } from 'lineicons-svelte';` |
| `ruler-pen` | `import { ruler-pen } from 'lineicons-svelte';` |
| `rupee` | `import { rupee } from 'lineicons-svelte';` |

### S

| Icon | Import |
|------|--------|
| `safari` | `import { safari } from 'lineicons-svelte';` |
| `sanity` | `import { sanity } from 'lineicons-svelte';` |
| `school-bench-1` | `import { school-bench-1 } from 'lineicons-svelte';` |
| `school-bench-2` | `import { school-bench-2 } from 'lineicons-svelte';` |
| `scissors-1-vertical` | `import { scissors-1-vertical } from 'lineicons-svelte';` |
| `scoter` | `import { scoter } from 'lineicons-svelte';` |
| `scroll-down-2` | `import { scroll-down-2 } from 'lineicons-svelte';` |
| `search-1` | `import { search-1 } from 'lineicons-svelte';` |
| `search-2` | `import { search-2 } from 'lineicons-svelte';` |
| `search-minus` | `import { search-minus } from 'lineicons-svelte';` |
| `search-plus` | `import { search-plus } from 'lineicons-svelte';` |
| `search-text` | `import { search-text } from 'lineicons-svelte';` |
| `select-cursor-1` | `import { select-cursor-1 } from 'lineicons-svelte';` |
| `seo-monitor` | `import { seo-monitor } from 'lineicons-svelte';` |
| `service-bell-1` | `import { service-bell-1 } from 'lineicons-svelte';` |
| `share-1` | `import { share-1 } from 'lineicons-svelte';` |
| `share-1-circle` | `import { share-1-circle } from 'lineicons-svelte';` |
| `share-2` | `import { share-2 } from 'lineicons-svelte';` |
| `shield-2` | `import { shield-2 } from 'lineicons-svelte';` |
| `shield-2-check` | `import { shield-2-check } from 'lineicons-svelte';` |
| `shield-dollar` | `import { shield-dollar } from 'lineicons-svelte';` |
| `shift-left` | `import { shift-left } from 'lineicons-svelte';` |
| `shift-right` | `import { shift-right } from 'lineicons-svelte';` |
| `ship-1` | `import { ship-1 } from 'lineicons-svelte';` |
| `shirt-1` | `import { shirt-1 } from 'lineicons-svelte';` |
| `shopify` | `import { shopify } from 'lineicons-svelte';` |
| `shovel` | `import { shovel } from 'lineicons-svelte';` |
| `shuffle` | `import { shuffle } from 'lineicons-svelte';` |
| `sign-post-left` | `import { sign-post-left } from 'lineicons-svelte';` |
| `signal-app` | `import { signal-app } from 'lineicons-svelte';` |
| `signs-post-2` | `import { signs-post-2 } from 'lineicons-svelte';` |
| `sketch` | `import { sketch } from 'lineicons-svelte';` |
| `skype` | `import { skype } from 'lineicons-svelte';` |
| `slack` | `import { slack } from 'lineicons-svelte';` |
| `slice-2` | `import { slice-2 } from 'lineicons-svelte';` |
| `sliders-horizontal-square-2` | `import { sliders-horizontal-square-2 } from 'lineicons-svelte';` |
| `slideshare` | `import { slideshare } from 'lineicons-svelte';` |
| `snapchat` | `import { snapchat } from 'lineicons-svelte';` |
| `sort-alphabetical` | `import { sort-alphabetical } from 'lineicons-svelte';` |
| `sort-high-to-low` | `import { sort-high-to-low } from 'lineicons-svelte';` |
| `soundcloud` | `import { soundcloud } from 'lineicons-svelte';` |
| `spacex` | `import { spacex } from 'lineicons-svelte';` |
| `spellcheck` | `import { spellcheck } from 'lineicons-svelte';` |
| `spinner-2-sacle` | `import { spinner-2-sacle } from 'lineicons-svelte';` |
| `spinner-3` | `import { spinner-3 } from 'lineicons-svelte';` |
| `sports` | `import { sports } from 'lineicons-svelte';` |
| `spotify` | `import { spotify } from 'lineicons-svelte';` |
| `spotify-alt` | `import { spotify-alt } from 'lineicons-svelte';` |
| `squarespace` | `import { squarespace } from 'lineicons-svelte';` |
| `stackoverflow` | `import { stackoverflow } from 'lineicons-svelte';` |
| `stamp` | `import { stamp } from 'lineicons-svelte';` |
| `star-fat` | `import { star-fat } from 'lineicons-svelte';` |
| `star-fat-half-2` | `import { star-fat-half-2 } from 'lineicons-svelte';` |
| `star-sharp-disabled` | `import { star-sharp-disabled } from 'lineicons-svelte';` |
| `statista` | `import { statista } from 'lineicons-svelte';` |
| `steam` | `import { steam } from 'lineicons-svelte';` |
| `stethoscope-1` | `import { stethoscope-1 } from 'lineicons-svelte';` |
| `stopwatch` | `import { stopwatch } from 'lineicons-svelte';` |
| `storage-hdd-2` | `import { storage-hdd-2 } from 'lineicons-svelte';` |
| `strikethrough-1` | `import { strikethrough-1 } from 'lineicons-svelte';` |
| `stripe` | `import { stripe } from 'lineicons-svelte';` |
| `stumbleupon` | `import { stumbleupon } from 'lineicons-svelte';` |
| `sun-1` | `import { sun-1 } from 'lineicons-svelte';` |
| `supabase` | `import { supabase } from 'lineicons-svelte';` |
| `surfboard-2` | `import { surfboard-2 } from 'lineicons-svelte';` |
| `svelte` | `import { svelte } from 'lineicons-svelte';` |
| `swift` | `import { swift } from 'lineicons-svelte';` |

### T

| Icon | Import |
|------|--------|
| `tab` | `import { tab } from 'lineicons-svelte';` |
| `tailwindcss` | `import { tailwindcss } from 'lineicons-svelte';` |
| `target-user` | `import { target-user } from 'lineicons-svelte';` |
| `telegram` | `import { telegram } from 'lineicons-svelte';` |
| `telephone-1` | `import { telephone-1 } from 'lineicons-svelte';` |
| `telephone-3` | `import { telephone-3 } from 'lineicons-svelte';` |
| `tesla` | `import { tesla } from 'lineicons-svelte';` |
| `text-format` | `import { text-format } from 'lineicons-svelte';` |
| `text-format-remove` | `import { text-format-remove } from 'lineicons-svelte';` |
| `text-paragraph` | `import { text-paragraph } from 'lineicons-svelte';` |
| `thumbs-down-3` | `import { thumbs-down-3 } from 'lineicons-svelte';` |
| `thumbs-up-3` | `import { thumbs-up-3 } from 'lineicons-svelte';` |
| `ticket-1` | `import { ticket-1 } from 'lineicons-svelte';` |
| `tickets-3` | `import { tickets-3 } from 'lineicons-svelte';` |
| `tiktok` | `import { tiktok } from 'lineicons-svelte';` |
| `tiktok-alt` | `import { tiktok-alt } from 'lineicons-svelte';` |
| `tower-broadcast-1` | `import { tower-broadcast-1 } from 'lineicons-svelte';` |
| `toyota` | `import { toyota } from 'lineicons-svelte';` |
| `train-1` | `import { train-1 } from 'lineicons-svelte';` |
| `train-3` | `import { train-3 } from 'lineicons-svelte';` |
| `trash-3` | `import { trash-3 } from 'lineicons-svelte';` |
| `tree-2` | `import { tree-2 } from 'lineicons-svelte';` |
| `trees-3` | `import { trees-3 } from 'lineicons-svelte';` |
| `trello` | `import { trello } from 'lineicons-svelte';` |
| `trend-down-1` | `import { trend-down-1 } from 'lineicons-svelte';` |
| `trend-up-1` | `import { trend-up-1 } from 'lineicons-svelte';` |
| `trophy-1` | `import { trophy-1 } from 'lineicons-svelte';` |
| `trowel-1` | `import { trowel-1 } from 'lineicons-svelte';` |
| `truck-delivery-1` | `import { truck-delivery-1 } from 'lineicons-svelte';` |
| `tumblr` | `import { tumblr } from 'lineicons-svelte';` |
| `turborepo` | `import { turborepo } from 'lineicons-svelte';` |
| `twitch` | `import { twitch } from 'lineicons-svelte';` |
| `twitter-old` | `import { twitter-old } from 'lineicons-svelte';` |
| `typescript` | `import { typescript } from 'lineicons-svelte';` |

### U

| Icon | Import |
|------|--------|
| `uber` | `import { uber } from 'lineicons-svelte';` |
| `uber-symbol` | `import { uber-symbol } from 'lineicons-svelte';` |
| `ubuntu` | `import { ubuntu } from 'lineicons-svelte';` |
| `underline` | `import { underline } from 'lineicons-svelte';` |
| `unlink-2-angular-eft` | `import { unlink-2-angular-eft } from 'lineicons-svelte';` |
| `unlocked-2` | `import { unlocked-2 } from 'lineicons-svelte';` |
| `unsplash` | `import { unsplash } from 'lineicons-svelte';` |
| `upload-1` | `import { upload-1 } from 'lineicons-svelte';` |
| `upload-circle-1` | `import { upload-circle-1 } from 'lineicons-svelte';` |
| `user-4` | `import { user-4 } from 'lineicons-svelte';` |
| `user-multiple-4` | `import { user-multiple-4 } from 'lineicons-svelte';` |

### V

| Icon | Import |
|------|--------|
| `vector-nodes-6` | `import { vector-nodes-6 } from 'lineicons-svelte';` |
| `vector-nodes-7` | `import { vector-nodes-7 } from 'lineicons-svelte';` |
| `vercel` | `import { vercel } from 'lineicons-svelte';` |
| `vimeo` | `import { vimeo } from 'lineicons-svelte';` |
| `visa` | `import { visa } from 'lineicons-svelte';` |
| `vite` | `import { vite } from 'lineicons-svelte';` |
| `vk` | `import { vk } from 'lineicons-svelte';` |
| `vmware` | `import { vmware } from 'lineicons-svelte';` |
| `volkswagen` | `import { volkswagen } from 'lineicons-svelte';` |
| `volume-1` | `import { volume-1 } from 'lineicons-svelte';` |
| `volume-high` | `import { volume-high } from 'lineicons-svelte';` |
| `volume-low` | `import { volume-low } from 'lineicons-svelte';` |
| `volume-mute` | `import { volume-mute } from 'lineicons-svelte';` |
| `volume-off` | `import { volume-off } from 'lineicons-svelte';` |
| `vs-code` | `import { vs-code } from 'lineicons-svelte';` |
| `vuejs` | `import { vuejs } from 'lineicons-svelte';` |

### W

| Icon | Import |
|------|--------|
| `wallet-1` | `import { wallet-1 } from 'lineicons-svelte';` |
| `watch-beat-1` | `import { watch-beat-1 } from 'lineicons-svelte';` |
| `water-drop-1` | `import { water-drop-1 } from 'lineicons-svelte';` |
| `webflow` | `import { webflow } from 'lineicons-svelte';` |
| `webhooks` | `import { webhooks } from 'lineicons-svelte';` |
| `wechat` | `import { wechat } from 'lineicons-svelte';` |
| `weight-machine-1` | `import { weight-machine-1 } from 'lineicons-svelte';` |
| `whatsapp` | `import { whatsapp } from 'lineicons-svelte';` |
| `wheelbarrow-empty` | `import { wheelbarrow-empty } from 'lineicons-svelte';` |
| `wheelchair-1` | `import { wheelchair-1 } from 'lineicons-svelte';` |
| `windows` | `import { windows } from 'lineicons-svelte';` |
| `wise` | `import { wise } from 'lineicons-svelte';` |
| `wordpress` | `import { wordpress } from 'lineicons-svelte';` |
| `www` | `import { www } from 'lineicons-svelte';` |
| `www-cursor` | `import { www-cursor } from 'lineicons-svelte';` |

### X

| Icon | Import |
|------|--------|
| `x` | `import { x } from 'lineicons-svelte';` |
| `xampp` | `import { xampp } from 'lineicons-svelte';` |
| `xbox` | `import { xbox } from 'lineicons-svelte';` |
| `xmark` | `import { xmark } from 'lineicons-svelte';` |
| `xmark-circle` | `import { xmark-circle } from 'lineicons-svelte';` |
| `xrp` | `import { xrp } from 'lineicons-svelte';` |

### Y

| Icon | Import |
|------|--------|
| `yahoo` | `import { yahoo } from 'lineicons-svelte';` |
| `yarn` | `import { yarn } from 'lineicons-svelte';` |
| `ycombinator` | `import { ycombinator } from 'lineicons-svelte';` |
| `yen` | `import { yen } from 'lineicons-svelte';` |
| `youtube` | `import { youtube } from 'lineicons-svelte';` |
| `youtube-kids` | `import { youtube-kids } from 'lineicons-svelte';` |
| `youtube-music` | `import { youtube-music } from 'lineicons-svelte';` |

### Z

| Icon | Import |
|------|--------|
| `zapier` | `import { zapier } from 'lineicons-svelte';` |
| `zero-size` | `import { zero-size } from 'lineicons-svelte';` |
| `zoom` | `import { zoom } from 'lineicons-svelte';` |


---

*Generated automatically. Total icons: 606*
